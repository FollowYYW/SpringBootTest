server:
  port: 10010 # 网关端口
spring:
  application:
    name: battle-gateway # 服务名称
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.5:8848 #注册中心地址
      config:
        server-addr: 192.168.1.5:8848 #Nacos配置中心地址
        file-extension: yml
        enabled: true